// Reference test for UseCase base. Boilerplate only - replace with your logic and data.
// TODO: Test custom use cases that extend UseCase; NoParams is a simple marker class.
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_boilerplate/core/usecases/usecase.dart';

void main() {
  group('UseCase / NoParams', () {
    test('placeholder - NoParams can be instantiated', () {
      expect(NoParams(), isNotNull);
    });
  });
}
