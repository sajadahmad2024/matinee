import { ExperienceForm } from "../_components/experience-form";

// Same form as edit, empty — "Create" saves as draft (mock).
export default function NewExperiencePage() {
  return <ExperienceForm />;
}
